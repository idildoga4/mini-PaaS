apiVersion: 1

providers:
  - name: 'Mini PaaS'
    folder: 'Mini PaaS'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards